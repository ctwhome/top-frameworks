{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [
      2,
      "always",
      [
        "postgres",
        "postgres-rls",
        "electric",
        "jazz",
        "tinybase",
        "root",
        "deps",
        "release"
      ]
    ],
    "body-max-line-length": [0, "always"]
  }
}